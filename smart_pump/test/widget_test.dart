import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:smart_pump/main.dart';

void main() {
  testWidgets('SmartPump smoke test', (WidgetTester tester) async {
    // Test vide - on verifie juste que ca compile
  });
}
